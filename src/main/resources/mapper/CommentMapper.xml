<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lxy.mapper.CommentMapper">
    <insert id="saveComment">
        insert into t_comment(id, nickname, email, content, avatar, create_time, blog_id, parent_comment_id,
                              admin_comment)
        VALUES(#{id},#{nickname},#{email},#{content},#{avatar},#{createTime},#{blog.id},#{parentComment.id},#{adminComment});

    </insert>
    <delete id="deleteByCommentId">
        delete
        from t_comment
        where id = #{id};
    </delete>

    <select id="getCommentsByBlogId" resultType="com.lxy.pojo.Comment">
        select *
        from t_comment
        where blog_id = #{blog_id}
        order by create_time desc;
    </select>
    <select id="getCommentByParentId" resultType="com.lxy.pojo.Comment">
        select *
        from t_comment
        where id = #{parentCommentId};
    </select>
</mapper>